# 项目约定

## Git

- **每次完成一次更新/修复后，都要提交 git，并 push 到 GitHub `origin/main`**（用户明确要求）。
- 提交信息使用简洁的 conventional 风格（`feat:` / `fix:` / `docs:` 等），中文或英文均可，与仓库历史保持一致。
- 不要提交 `.pixi/`、`__pycache__/`、音频临时文件等。
